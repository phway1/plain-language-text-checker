[project]
name = "plain-language-text-checker"
version = "0.1.0"
description = "Transparent local plain-language checks"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py310"

